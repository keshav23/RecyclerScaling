package com.example.recyclerviewscaling

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class AudioEmptySlotViewHolder(private val view: View) :
    RecyclerView.ViewHolder(view) {
}